---
name: ukraine-alarm
description: Отримання інформації про повітряні тривоги в Україні (Ukrainian Alarm API)
homepage: https://api.ukrainealarm.com
metadata: {"openclaw":{"emoji":"🚨","requires":{"bins":["curl"],"env":["UKRAINE_ALARM_API_KEY"]},"primaryEnv":"UKRAINE_ALARM_API_KEY"}}
---

# Ukraine Alarm - Повітряні тривоги в Україні

Скіл для отримання інформації про повітряні тривоги в Україні через Ukrainian Alarm API.

## Базовий URL API

```
https://api.ukrainealarm.com/api/v3
```

## Автентифікація

Всі запити потребують API ключ в заголовку:

```bash
Authorization: $UKRAINE_ALARM_API_KEY
```

## Основні команди

### 1. Отримати всі активні тривоги

```bash
curl -s "https://api.ukrainealarm.com/api/v3/alerts" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" \
  -H "Accept: application/json"
```

Повертає список всіх областей, регіонів та громад з активними тривогами.

**Формат відповіді:**
```json
[
  {
    "regionId": "9",
    "regionName": "Київська область",
    "regionType": "State",
    "activeAlerts": ["AIR"],
    "lastUpdate": "2024-02-28T10:00:00Z"
  }
]
```

**Типи тривог:**
- `AIR` - повітряна тривога
- `ARTILLERY` - артилерійський обстріл
- `URBAN_FIGHTS` - вуличні бої
- `CHEMICAL` - хімічна загроза
- `NUCLEAR` - ядерна загроза

### 2. Перевірити статус конкретного регіону

```bash
curl -s "https://api.ukrainealarm.com/api/v3/alerts/{regionId}" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" \
  -H "Accept: application/json"
```

Приклад (Київська область - ID: 9):
```bash
curl -s "https://api.ukrainealarm.com/api/v3/alerts/9" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" \
  -H "Accept: application/json"
```

**Формат відповіді:**
```json
{
  "regionId": "9",
  "regionName": "Київська область",
  "regionType": "State",
  "activeAlerts": ["AIR"],
  "lastUpdate": "2024-02-28T10:00:00Z"
}
```

### 3. Отримати історію тривог для регіону

```bash
curl -s "https://api.ukrainealarm.com/api/v3/alerts/regionHistory?regionId={regionId}" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" \
  -H "Accept: application/json"
```

Приклад:
```bash
curl -s "https://api.ukrainealarm.com/api/v3/alerts/regionHistory?regionId=9" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" \
  -H "Accept: application/json"
```

Повертає останні 25 тривог для вказаного регіону.

**Формат відповіді:**
```json
[
  {
    "regionId": "9",
    "regionName": "Київська область",
    "alertType": "AIR",
    "startDate": "2024-02-28T08:00:00Z",
    "endDate": "2024-02-28T09:30:00Z"
  }
]
```

### 4. Отримати список всіх регіонів

```bash
curl -s "https://api.ukrainealarm.com/api/v3/regions" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" \
  -H "Accept: application/json"
```

Повертає повний список областей, міст та громад з їх ID.

**Формат відповіді:**
```json
[
  {
    "regionId": "9",
    "regionName": "Київська область",
    "regionType": "State"
  },
  {
    "regionId": "31",
    "regionName": "м. Київ",
    "regionType": "City"
  }
]
```

**Типи регіонів:**
- `State` - область
- `City` - місто
- `District` - район
- `Community` - громада

### 5. Перевірити номер останньої дії

```bash
curl -s "https://api.ukrainealarm.com/api/v3/alerts/status" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" \
  -H "Accept: application/json"
```

Використовується для перевірки, чи були оновлення в системі тривог.

**Формат відповіді:**
```json
{
  "lastActionIndex": 123456
}
```

## Форматування виводу

Для кращої читабельності JSON використовуйте `jq`:

```bash
# Відформатований вивід
curl -s "https://api.ukrainealarm.com/api/v3/alerts" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" | jq '.'

# Витягти тільки назви регіонів з тривогами
curl -s "https://api.ukrainealarm.com/api/v3/alerts" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" | jq -r '.[].regionName'

# Підрахувати кількість активних тривог
curl -s "https://api.ukrainealarm.com/api/v3/alerts" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" | jq '. | length'
```

## Популярні ID регіонів

| Регіон | ID |
|--------|-----|
| м. Київ | 31 |
| Київська область | 9 |
| Харківська область | 20 |
| Одеська область | 14 |
| Дніпропетровська область | 4 |
| Львівська область | 13 |
| Донецька область | 7 |
| Запорізька область | 22 |
| Вінницька область | 5 |
| Полтавська область | 16 |

Повний список ID дивіться в файлі `REGIONS.md`.

## Обробка помилок

**401 Unauthorized:**
```bash
# Перевірте, чи встановлено змінну оточення
echo $UKRAINE_ALARM_API_KEY

# Якщо порожньо, встановіть її
export UKRAINE_ALARM_API_KEY="your_api_key_here"
```

**404 Not Found:**
- Перевірте правильність ID регіону
- Використайте endpoint `/regions` для отримання списку доступних ID

**429 Too Many Requests:**
- API має ліміт запитів
- Зачекайте перед наступним запитом
- Кешуйте результати для частих запитів

**Timeout:**
```bash
# Додайте таймаут до curl
curl -s --max-time 10 "https://api.ukrainealarm.com/api/v3/alerts" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY"
```

## Практичні приклади

**Перевірити, чи є тривоги зараз:**
```bash
alerts=$(curl -s "https://api.ukrainealarm.com/api/v3/alerts" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY")

if [ "$(echo $alerts | jq '. | length')" -gt 0 ]; then
  echo "🚨 Активні тривоги!"
  echo $alerts | jq -r '.[].regionName'
else
  echo "✅ Тривог немає"
fi
```

**Моніторинг конкретного міста:**
```bash
# Київ (ID: 31)
watch -n 60 'curl -s "https://api.ukrainealarm.com/api/v3/alerts/31" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" | jq "."'
```

**Статистика тривог за сьогодні:**
```bash
curl -s "https://api.ukrainealarm.com/api/v3/alerts/regionHistory?regionId=9" \
  -H "Authorization: $UKRAINE_ALARM_API_KEY" | \
  jq '[.[] | select(.startDate | startswith("2024-02-28"))] | length'
```

## Інтеграція з OpenClaw

Коли користувач запитує про тривоги:

1. Для загального запиту ("Чи є тривоги?") - використовуйте `/alerts`
2. Для конкретного регіону ("Тривога в Києві?") - використовуйте `/alerts/{regionId}`
3. Для історії ("Скільки разів була тривога?") - використовуйте `/alerts/regionHistory`
4. Для пошуку ID - використовуйте `/regions`

**Важливо:**
- Завжди форматуйте відповіді для користувача зручно (не просто JSON)
- Показуйте емодзі для наочності (🚨 тривога, ✅ немає тривоги)
- Вказуйте час останнього оновлення

## Безпека

- **Ніколи** не показуйте API ключ користувачу
- Не логуйте API ключ в консоль
- Зберігайте ключ тільки в змінних оточення або конфігурації OpenClaw
- Не комітьте файли з ключами в Git

## Довідка

- API документація: https://api.ukrainealarm.com/swagger/index.html
- Повний список регіонів: див. файл `REGIONS.md`
- Приклади використання: див. файл `EXAMPLES.md`

## Підтримка

Якщо виникли проблеми:
1. Перевірте наявність API ключа: `echo $UKRAINE_ALARM_API_KEY`
2. Перевірте з'єднання: `curl -I https://api.ukrainealarm.com`
3. Перевірте curl та jq: `which curl jq`
4. Перегляньте лог помилок
